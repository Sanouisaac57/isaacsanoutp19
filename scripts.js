// on va chercher les différents élements de notre page
const pages = document.querySelectorAll(".page")
const header = document.querySelector("header")
const nbPages = pages.length //nombres de page du formulaire

console.log(nbPages)